# scene bundles are generated by cramera-onboard (tens of MB each); ready-made
# recordings come from the *optional* cram-scenes submodule at ./scenes — see
# src/cramera/paths.py for the search order
